tap "homebrew/bundle"

# === CLI tools ===
brew "ripgrep"      # rg — fast content search
brew "bat"          # cat with syntax highlighting
brew "eza"          # modern ls
brew "fd"           # modern find
brew "fzf"          # fuzzy finder
brew "zoxide"       # z — smart directory jumping
brew "jq"           # JSON processor
brew "starship"     # shell prompt
brew "gh"           # GitHub CLI
brew "direnv"       # per-directory env vars
brew "lazygit"      # git TUI
brew "tmux"         # terminal multiplexer
brew "duckdb"       # in-process analytics DB
brew "btop"         # system monitor

# === Apps (casks) ===
cask "ghostty"                        # terminal
cask "visual-studio-code"             # editor
cask "zed"                            # editor — fast, Rust-based
cask "orbstack"                       # Docker/Linux VMs, lightweight
cask "rectangle"                      # window management
cask "raycast"                        # Spotlight replacement
cask "font-jetbrains-mono-nerd-font"  # Nerd Font for starship icons

# Note: uv is installed via its own installer (astral.sh), not Homebrew,
# so it is intentionally not listed here.
